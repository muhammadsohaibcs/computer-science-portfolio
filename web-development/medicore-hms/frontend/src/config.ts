const config = {
  apiBaseUrl: import.meta.env.VITE_API_BASE_URL || 'http://localhost:4000',
  appName: import.meta.env.VITE_APP_NAME || 'MediCore HMS',
  appVersion: '2.0.0',
};
export default config;
